# netbox-snmp-plugin
NetBox 4.x plugin for querying devices and interfaces via SNMP (v2c/v3) — compare live data with NetBox and sync fields like description, status, speed, MTU and MAC. Includes bulk polling, 64-bit counters and a draggable modal UI.
